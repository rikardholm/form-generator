package formgen.tasks.reports.valuestatement;

import java.util.concurrent.Callable;

public class ValueStatementReport implements Callable<byte[]> {
    @Override
    public byte[] call() throws Exception {
        return new byte[0];
    }
}
